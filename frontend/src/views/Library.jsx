/**
 * Library view — displays all tracks with filtering, sorting, and grouping.
 * Implemented in Phase 4.
 */

export default function Library() {
  return <div>Library view — coming in Phase 4</div>;
}
